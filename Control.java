package easyRideControl;

public class Control {
/*
we should have it so that if driver is true passenger has to be set to false in the model! and vice versa
 */
	boolean driverButton(String userName)
	{
		boolean driver = true;
		return driver;
	}
	
	boolean passengerButton(String userName)
	{
		boolean passenger = true;
		return passenger;
	}
	
}
